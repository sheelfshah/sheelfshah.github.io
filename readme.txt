all useful info

1. intern and ss blogs have dynamic styling
2. this needs to be done for grad school too
3. footer is dynamically loaded
4. run git remote add server ssh://eesa@10.107.1.2/ug/misc/eesa/public_html (needed only the first time)
5. git push to push to github(i.e. origin)
6. git push server master (using vpn)
7. git checkout master(at server)
   git reset --hard #for now
8. always place a copy of index in preview